// Setup global para testes de integração
// Este arquivo é executado antes de todos os testes de integração

// Configurações globais para testes de integração
beforeAll(async () => {
  // Setup inicial se necessário
});

afterAll(async () => {
  // Cleanup final se necessário
});

// Configurações de timeout para testes de integração
jest.setTimeout(10000);
